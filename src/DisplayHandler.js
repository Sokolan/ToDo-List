export default class DisplayHandler {
  #createHeader() {
    const header = document.createElement('div');
    header.
  }
  createDisplay() {
    createHeader();
    createBody();
    createFooter();
  }
}